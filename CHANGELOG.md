# OpenJob 更新记录

这里记录用户可以感知的功能、体验和稳定性变化。版本号与项目当前发布版本保持一致；同一版本内的持续优化按日期分别记录。

| 日期 | 版本 | 类型 | 更新内容 |
| --- | --- | --- | --- |
| 2026-09-19 | Unreleased | BOSS 推荐页采集 | 新增 BOSS 直聘推荐页岗位采集（默认关闭，采集窗口可勾选开启）：推荐页与搜索流共用同一套去重、过滤、详情解析、入库、AI 评分与页面额度（推荐页每日加载上限 10，仍受全局 500 页约束），同一岗位同时来自搜索流与推荐页时保留双来源标记（岗位池显示来源徽标），采集弹窗可配置分页轮次/最大卡片数/连续无新增上限，定时采集默认不含推荐页可显式开启。推荐页只扩大岗位发现范围，不改变人工投递控制权，不会自动发送任何消息。 |
| 2026-09-14 | Unreleased | 深度与效率 | 新增求职周报：`openjob weekly` 预览上周数字（新增/过线/发送/回复+环比）与队列健康待办，dry-run 制——加 `--write --yes` 才写入归档目录（默认 data/weekly_reports/，可配置到 Obsidian 库）；工作台同款预览/写入接口。新增全量数据导出：`openjob export-data` 打包数据库快照+简历产物+素材库+用量周报（白名单制，绝不含 config.yaml 与 API Key，可迁移到新目录恢复）。招呼语生成时记录客观风格特征（长度/开头/是否问句结尾），新增效果粗对比接口并明示"相关≠因果"。 |
| 2026-09-14 | Unreleased | 工程质量 | 新增 `openjob doctor` 自诊断：Chrome 连接、AI 配置、配置完整性、数据库完整性（PRAGMA quick_check）、磁盘空间、备份新鲜度、风控锁、定时采集八项体检，每项带一行修复建议；`--package` 可导出脱敏诊断包（默认不含 API Key/简历/联系方式，日志需显式 --include-logs 且电话邮箱自动脱敏）。新增 `openjob restore <备份>` 一键恢复：坏备份直接拒绝、恢复前自动另存当前库、原子替换；数据库版本号（PRAGMA user_version）落章，旧库升级前自动备份一份。监测页新增「运行记录」面板：采集/评分运行历史、耗时与错误归因聚合（选择器失效/AI 解析/事实拦截等分类计数）；任务日志按任务 ID 持久化到 data/logs/（5MB 轮转），`/api/logs/<id>/tail` 可查尾部 200 行。CI 新增核心模块覆盖率门禁（基线 72%，只增不降）；新增端到端冒烟测试（seed → API → 状态迁移断言）；修复批量过期退出对不存在岗位误计数的问题。 |
| 2026-09-14 | Unreleased | 投后闭环 | 打通"发送之后"的黑洞：HR 回复自动回流（首响时间/回复次数/最新摘要落库，岗位池显示回复徽章，桌面通知带岗位与消息上下文）；未匹配或歧义会话不再丢弃，进入新增的第 8 页「回复工作台」，支持人工关联岗位、生成回复草稿（只复制到剪贴板、绝不自动发送，草稿过事实校验且 HR 消息做注入防御）；新增投后终态管理：约面试 / Offer / HR 拒绝 / 关闭（含原因）手动流转，岗位详情新增事件时间线；确认队列治理积压：卡片显示已等待天数、超 7 天灰显提醒、一键批量"过期退出"（可重新激活），批量投递改为六要素确认对话框（发送数/平台分布/事实校验/需检查/剩余额度/不可撤回警示）。 |
| 2026-09-14 | Unreleased | 契约冻结与事实安全 | 新增产品契约单一来源（状态标签与迁移白名单、平台能力、事实来源类型、任务生命周期，见 docs/developer-contract.md 与 GET /api/contracts），非法岗位状态迁移会被拒绝并保留原状态（409）；计划采集重启收敛改为 interrupted，后台任务终态记录 finished_at 与错误数。事实安全升级为代码级强制：三处示例黑名单收敛为一份（fact_policy），底稿/素材以脱敏事实片段进入 AI 上下文（电话、邮箱、来源与备注不再发送给 AI），岗位 JD 作为不可信文本做注入中性化并记录风险标记；发送边界在原 verified 门之上新增逐条事实复检（来源缺失或文本被改动即拦截，记录 send_blocked_fact_recheck）；新增用户编辑再校验（工作台编辑接口与 CLI 确认一致，未通过校验的文本不可发送并提示补素材）。 |
| 2026-09-09 | Unreleased | 工作台与产品化 | 新增工作台内定时采集：在工作台和专用 Chrome 存活期间按时执行“采集 + AI 评分”，不外发、不补跑；新增计划任务状态与持久化防重复执行。补齐任意 XLSX 简历素材库智能导入（分析、字段映射、预览、确认），并增加 BOSS 详情页一次性重试、采集/评分实时日志与 AI 评分进度。CI 现执行 Python 测试、静态检查、前端构建与 UI 验收；README 与产品指南同步说明业务定位、使用方式、能力边界与安全约束。 |
| 2026-09-03 | v1.1.1 | 工程优化 | DashboardPage 拆分：岗位池/监测页独立成文件（主文件 1280→660 行）；图表组件懒加载，recharts 拆出独立分包，工作台主 chunk 492KB→48KB（gzip 136→14KB）；UI 验收脚本转正为前端内 scripts/ui-acceptance.cjs。 |
| 2026-09-03 | v1.1.0 | UI 2.0 | 全站视觉重构：悬浮 App Shell、窄图标栏、胶囊导航、首页 Bento 化（Hero/周活动/自动化控制/流程进度/事项卡/最佳匹配/趋势/用量环）；岗位池表格重构（主信息列合并+评分徽章）；确认页分页+快捷筛选+吸顶批量栏；移动端底部导航与全页适配；弹窗 Portal 化（Esc/遮罩关闭/焦点管理）；新增 scripts/ui-acceptance.cjs 多视口验收脚本。 |
| 2026-09-01 | v1.0.0 | 首次发布 | 配置向导上线；三方向底稿统一一页标准；JD 定向改写与排版零损伤渲染管线加固（逐行字数预算、超长回退、假改写与 AI 腔过滤）；GitHub 开源。 |
| 2026-08-31 | v0.2.0 | 定制简历 | 一页标准底稿制作与微调（证件照/加粗/居中）、88 分岗位真实生成全链路验收、市场分析页支持按范围统计、推荐页采集并入日常流程。 |
| 2026-08-29 | v0.1.0 | 简历引擎与体验 | 上线简历优化引擎（JD 解析→匹配分析→分段改写→人工审阅→导出）、多方向底稿自动选稿、桌面通知、市场分析页、AI 用量面板、每日数据库备份、拒绝拉黑公司、实习/正式岗筛选、明暗主题与界面焕新（Lighthouse 性能 98）。 |
| 2026-08-28 | v0.1.0 | 链路验收 | 默认 AI 切换 DeepSeek，真实环境完成采集→评分→确认→发送→监听闭环；修复预设招呼语弹窗导致的重复打招呼（会话级去重守卫 + 弹窗文案替换）。 |

## v1.0.0

### 新增与改进

- 配置向导 `scripts/setup.py`：交互式生成 `config.yaml`；API Key 本地存储，支持环境变量注入。
- 三方向底稿全部统一到一页标准：正文 9.5pt、行距 0.95、栏目间距压缩、教育行加粗与居中、证件照位置固化。
- 渲染管线加固：固定区白名单（照片/栏目标题/联系方式永不改写）、逐行字数预算、超长行回退、假改写与 AI 腔双重过滤、两段式校验（编造零容忍先于长度兜底）。
- 定向猎捕批次与推荐页采集并入日常采集流程，采集前招聘类型过滤生效。

### 验证

- 468 项测试通过；真实环境以 88 分岗位完成采集 → 生成 → 导出全链路验收。

## v0.2.0

### 新增与改进

- 一页标准底稿制作：证件照替换与比例对齐、教育行加粗与居中、联系方式与栏目标题固化。
- 真实岗位生成验收：JD 定向改写、一页导出、AI 腔零残留。
- 市场分析页支持按采集范围统计岗位画像。

### 验证

- 渲染器行序列与提取器互逆验证：29/29 行回提一致。

## v0.1.0

### 新增与改进

- 简历优化引擎四段流水：JD 解析 → 匹配分析 → 分段改写 → 人工审阅 → 导出。
- 多方向简历底稿自动选稿：按岗位方向标签与名称命中，字符相似度兜底。
- 桌面通知：HR 回复、任务完成、异常中断即时提醒。
- 市场分析页、AI 用量面板、每日数据库自动备份。
- 拒绝即拉黑公司、实习/正式岗筛选、明暗双主题与界面焕新。

### 验证

- 真实环境完成采集 → 评分 → 确认 → 发送 → 监听闭环验收。
- 预设招呼语弹窗导致的重复打招呼问题修复（会话级去重守卫 + 弹窗文案替换）。
