# OpenJob「任意 Excel 智能导入」实施计划

> **给执行 Agent：请完整阅读并执行本计划。**
>
> 目标不是做一个只适配作者个人表格的演示，而是让任何用户从 GitHub 拉取 OpenJob 后，下载模板或直接选择自己已有的 Excel，经过“分析 → 映射 → 预览 → 用户确认”后，都能安全地成为简历素材库，并实际参与 JD 匹配和简历优化。
>
> 不要只修改 UI。必须实现后端解析、字段映射、预览确认、原子落盘、旧流程兼容、简历生成接入和自动化测试。

---

## 1. 最终产品目标

用户可以上传以下任意一种常见个人经历表：

- OpenJob 标准模板；
- 自己设计的中文表格，例如“经历”“项目”“奖项”“学生工作”“证书”分多个 Sheet；
- 中英文混合表头、列顺序不同、表头前有说明行或标题行的表格；
- 一张表里混合实习、项目、竞赛、学生工作和技能证据；
- 旧版 `.xls` 工作簿（如果运行环境安装了对应解析依赖）。

系统不能要求用户必须填写固定的 `素材库` Sheet，也不能要求必须使用当前 16 个英文列名。

但“任意 Excel”不等于“任何文件都无需确认、AI 猜完就直接写入简历”。可靠的产品流程必须是：

```text
选择 Excel
    ↓
扫描工作簿：文件、Sheet、表头、数据区、异常
    ↓
自动推断字段映射、素材类型和可用行
    ↓
用户查看预览，可修改映射和排除行
    ↓
用户确认导入
    ↓
转换为 OpenJob 标准素材库 + 索引
    ↓
JD 匹配候选素材
    ↓
AI 只能从已确认素材中改写，用户仍然人工审阅
```

### 1.1 本次必须达成的结果

- 其他用户不使用作者的个人 XLSX，也可以完成导入和简历优化；
- 现有标准模板仍然可用，已有用户不被迫迁移；
- 上传不再因为 Sheet 名称或列顺序不同而直接失败；
- 用户在确认前能看到“原始列 → OpenJob 字段”的映射及实际数据预览；
- 映射不确定、数据缺失、重复 ID、无法识别的列必须可见；
- 未确认的文件不能覆盖当前合法素材库；
- 导入的事实只能来自用户 Excel，任何 AI 都不得创造经历、数字、时间、组织、奖项等级或成果；
- 导入完成后，素材确实被简历生成引擎读取、匹配、审计和引用；
- 所有个人 Excel、索引、导入暂存文件和简历素材快照只保存在本机 `data/`。

### 1.2 明确不做的事

- 不自动替用户编造缺失的经历描述；
- 不自动投递、自动发送或绕过现有人工确认；
- 不把 `source`、`notes`、证明路径或私密备注发给 AI；
- 不因为“智能导入”而删除当前标准解析器和旧接口；
- 不把个人真实素材、上传文件或测试样本提交进 Git；
- 不把“导入成功”误认为“简历一定采用了全部素材”。素材仍然要经过 JD 匹配和生成阶段的选择。

---

## 2. 对当前项目的已确认问题

以下问题是基于当前仓库代码核对后整理的，修复时不要遗漏。

### 2.1 简历引擎与 API 的素材路径可能不一致

当前 Web API 已通过 `resolve_library_paths()` 读取配置路径，但 `src/openjob/ai/resume_engine/engine.py` 的素材加载阶段仍存在直接拼接默认路径的实现：

```python
materials_path = (data_dir / "resume_materials.xlsx").resolve()
index_path = (data_dir / "resume_materials.index.json").resolve()
```

这会导致用户在配置中设置自定义文件名后，上传/预览读取 A 文件，简历生成读取 B 文件。必须统一为同一公共路径解析函数，并为 API、engine、刷新、删除补回归测试。

### 2.2 当前解析器是固定模板解析器，不是任意 Excel 解析器

`src/openjob/resume_materials.py::parse_workbook()` 当前要求：

- 必须存在名为 `素材库` 的 Sheet；
- 第 1 行必须是表头；
- 必须拥有固定 16 个英文列名；
- `type` 必须是固定英文枚举；
- `id`、`description`、`resume_allowed` 等字段必须由用户直接提供。

这适合标准模板，但会拒绝“经历”“项目”“奖项”分 Sheet、中文表头、表头前有说明行、没有稳定 ID 的普通 Excel。不能直接把 `parse_workbook()` 改成大量脆弱的特判；应增加独立的“智能扫描/映射/转换”层，并保留标准解析器作为最终严格校验器。

### 2.3 当前上传接口是一步直写，没有确认阶段

当前 `POST /api/resume/materials/upload` 收到文件后直接 `parse_workbook()`，成功就原子覆盖正式 XLSX 和 index。任意 Excel 必须新增暂存和确认流程：

- 分析失败不能覆盖旧库；
- 分析成功但用户未确认，不能覆盖旧库；
- 确认时必须重新校验暂存文件哈希，防止用户在预览后替换文件；
- 确认失败时旧库和旧索引保持不变。

### 2.4 模板示例行不能变成用户真实素材

当前模板包含一行示例数据，现有测试也明确验证模板可解析为 1 条素材。这个行为会让用户下载模板后只填写自己的行、忘记删除示例时，示例经历进入候选集。

改造时应在模板中加入机器可识别的示例标记（推荐 `id=__openjob_example__` 或隐藏元数据），导入时默认排除并在预览中提示“已忽略示例行”。同时保留旧模板兼容规则：标题、描述带有明确“示例/请替换”标记的行不得进入正式库。

### 2.5 状态 API 可能展示旧索引，用户不知道是否已刷新

当前已经有 `stale` 和源文件 hash 的雏形，但需要把状态、预览、刷新和生成行为统一：

- XLSX 被外部编辑后，状态应显示“索引过期”；
- 旧索引可以继续只读展示，但必须明确这是旧预览；
- 用户点击刷新并成功解析后才更新正式索引；
- 刷新失败不能破坏原合法索引；
- 简历生成如果发现 hash 不一致，应使用当前 XLSX 重新解析；解析失败要明确失败，不能静默使用陈旧数据。

### 2.6 删除路径边界检查不能使用字符串前缀

当前删除逻辑仍有类似以下风险写法：

```python
str(path).startswith(str(DATA_DIR))
```

`data2` 可能被误判为 `data` 子路径。所有读、写、刷新、删除前都必须用 `Path.resolve()` + `relative_to()` 做目录边界校验。素材路径只能位于当前项目 `data/` 内。

### 2.7 转换脚本仍不是通用导入器

`scripts/convert_user_materials.py` 虽然已经有 CLI 外形，但它仍假定固定的三张中文 Sheet，并且在缺描述时拼出“详见成果”等兜底文本。这会制造看似完整、实际没有事实依据的内容。

脚本应复用新的智能扫描/映射层，支持 `--analyze`、`--mapping` 或至少能处理常见 Sheet/表头变化；缺少事实描述时应警告或让用户确认，不得默默编造占位内容。

---

## 3. 设计原则与兼容策略

### 3.1 两层解析架构

不要让智能导入直接绕过现有标准校验。采用两层：

1. **Smart Scanner / Import Analyzer**
   - 面向未知 Excel；
   - 找 Sheet、表头、数据行、列语义；
   - 输出映射、置信度、警告和预览；
   - 不写正式素材库。

2. **Canonical Normalizer / Strict Validator**
   - 把用户确认后的映射转换为现有 16 字段标准记录；
   - 复用或调用 `parse_workbook()` 的最终规则；
   - 校验 ID、类型、日期、布尔值、文本长度、重复项；
   - 通过后才原子保存 XLSX 和 index。

这样可以让“输入格式灵活”和“落库格式稳定”同时成立。

### 3.2 兼容旧模板和旧 API

- `parse_workbook()` 保留，标准 `素材库` + 16 列文件走快速识别路径，但前端仍显示预览和确认；
- 现有 `POST /api/resume/materials/upload` 不要无故删除。为旧脚本保留兼容行为，但新前端必须走分析/确认流程；
- 如果为了安全决定让旧 upload 也必须确认，返回字段必须兼容旧客户端，并提供迁移提示；
- 已有 index 的 schema、简历版本审计字段和 `material_ids` 规则不得被破坏；
- `resume_materials_enabled=false` 时，旧的无素材库生成行为保持不变；
- 开关打开但未导入素材时，继续兼容使用简历底稿，并在 UI 明确说明“当前没有额外素材”。

---

## 4. 统一标准数据模型

正式素材库继续使用当前标准字段，不让 AI 直接依赖用户任意列名：

| 标准字段 | 规则 | 智能导入策略 |
|---|---|---|
| `id` | 稳定、唯一 | 有用户 ID 就保留；没有则基于 Sheet + 行内容生成稳定 ID，并在预览标记“系统生成” |
| `type` | `experience/project/award/student_work/campus_activity/skill_evidence/certification/other` | 由映射列、Sheet 名、分类值和标题关键词推断，可由用户修改 |
| `title` | 1-120 字符 | 从名称/项目名/奖项名/证书名/岗位名映射 |
| `organization` | 可选 | 公司、学校、主办方、组织、机构 |
| `role` | 可选 | 职位、角色、担任身份、负责方向 |
| `start_date` / `end_date` | `YYYY`、`YYYY-MM`、`至今` | 识别常见 `2024.03`、`2024/03`、日期单元格，无法判断时保留原值并警告 |
| `description` | 必须有事实正文 | 映射“描述/职责/工作内容/项目内容/STAR”；可把多个明确事实列拼接，不得由 AI 编写新事实 |
| `achievements` | 可选 | 成果、结果、业绩、排名、获奖等级、数据 |
| `skills` | 可选列表 | 技能、工具、方法、技术栈；支持逗号、顿号、分号、换行，不应按 `/` 粗暴拆分版本号或短语 |
| `keywords` | 可选列表 | 关键词、标签、岗位关键词；可从已映射技能和方向生成去重副本 |
| `target_directions` | 可选列表 | 适用岗位、方向、投递方向 |
| `source` | 不进入 AI | 证明材料、链接、文件路径、核验来源 |
| `resume_allowed` | 必须可判断 | 有明确列就映射；缺失时在确认页让用户选择默认值，并标记默认来源 |
| `priority` | 1-5 | 有星级/重要程度/优先级就转换，否则默认 3 并提示 |
| `notes` | 不进入 AI | 备注、私密说明、维护备注 |

### 4.1 不得凭空补全的字段

以下情况必须显示警告或让用户修正，不能由 AI 虚构：

- 没有标题；
- 没有任何描述、职责、过程、成果或可验证事实；
- 日期只有模糊字符串且无法安全解析；
- 数字、比例、排名、公司名或奖项级别出现在无法确定语义的列；
- 一行可能包含两条素材但系统无法判断拆分边界；
- 同一 ID 对应不同内容；
- 合并单元格导致多行继承关系不明确。

缺失描述时可以把“职责 + 方法 + 成果”这些用户原始单元格按标签拼成描述，但必须在预览中标记“由原始列拼接”，绝不能生成“详见成果”之类的假描述。

---

## 5. 智能扫描与字段映射

### 5.1 新增领域模块

建议在 `src/openjob/resume_materials.py` 或拆分为同目录模块，实现以下公共对象和函数。命名可调整，但职责必须保留：

```python
@dataclass
class ImportSession:
    import_id: str
    filename: str
    source_sha256: str
    staged_path: Path
    sheets: list[dict]
    warnings: list[str]

def analyze_workbook(content: bytes, *, filename: str) -> ImportSession: ...
def infer_header_row(rows: list[list[object]]) -> int | None: ...
def infer_column_mapping(headers: list[str], sample_rows: list[list[object]], *, sheet_name: str) -> dict: ...
def normalize_import_rows(session: ImportSession, mapping: dict) -> tuple[list[dict], list[str]]: ...
def commit_import(session: ImportSession, mapping: dict, *, xlsx_path: Path, index_path: Path) -> MaterialLibrary: ...
```

不要要求 Agent 完全照抄函数签名；但分析、映射、转换、提交必须可单元测试，不能全部埋在 Bottle 路由中。

### 5.2 表头定位

- 扫描每个可见 Sheet 的前 30 行，而不是只看第 1 行；
- 用非空单元格数量、别名命中数、文本/日期/数值列分布判断表头；
- 处理表头前的标题、说明、更新时间、空行；
- 去除首尾空格，统一全角半角标点、换行和大小写；
- 合并表头允许拼接为 `上级表头_子表头`，但预览中必须显示原始列名；
- 找不到可靠表头时，该 Sheet 进入“需人工指定表头行”，不能静默把第一行当表头。

### 5.3 表头别名

建立可维护的别名表，至少覆盖：

- 标题：`title`、`名称`、`名称/标题`、`项目名称`、`经历名称`、`奖项名称`、`证书名称`、`职位`；
- 描述：`description`、`描述`、`详细描述`、`工作内容`、`职责`、`项目内容`、`STAR`、`做了什么`；
- 成果：`achievements`、`成果`、`量化成果`、`结果`、`业绩`、`排名`、`获奖等级`；
- 组织：`organization`、`公司`、`单位`、`学校`、`组织/机构`、`颁发机构`、`主办方`；
- 角色：`role`、`角色`、`职位`、`职务`、`担任身份`；
- 日期：`start_date`、`开始时间`、`起始时间`、`入职时间`，以及 `end_date`、`结束时间`、`离职时间`、`有效期至`、`获奖时间`；
- 技能：`skills`、`技能`、`工具`、`技术栈`、`关键技能`、`方法`；
- 方向：`target_directions`、`适用岗位`、`投递方向`、`求职方向`、`岗位类型`；
- 关键词：`keywords`、`关键词`、`标签`、`Tags`；
- 来源/备注：`source`、`证明材料`、`链接`、`备注`、`私密备注`；
- 控制字段：`id`、`编号`、`序号`、`是否写入简历`、`可用于简历`、`重要程度`、`优先级`。

映射必须输出：

```json
{
  "source_column": "详细描述（STAR 法则）",
  "target_field": "description",
  "confidence": 0.96,
  "reason": "命中中文别名",
  "sample_values": ["...", "..."]
}
```

### 5.4 Sheet 和类型推断

类型推断按以下优先级执行：

1. 用户在确认页明确指定；
2. 有类型/类别列且值能映射；
3. Sheet 名称命中：经历/实习 → `experience`，项目 → `project`，奖项/竞赛 → `award`，学生工作/社团/班委 → `student_work`，校园活动/志愿 → `campus_activity`，证书/证明 → `certification`；
4. 标题和内容关键词推断；
5. 无法判断时使用 `other`，并显示低置信度警告。

禁止仅凭一个词就把“项目经历”误判成项目而丢失实习属性；预览必须展示每个 Sheet/行的类型来源和可修改控件。

### 5.5 ID 生成

- 用户提供的合法唯一 ID 原样保留；
- 没有 ID 时生成 `imp_<sheet_slug>_<row_hash>`；`row_hash` 基于规范化后的原始行内容计算，确保同一个文件重复导入时 ID 稳定；
- 不允许只用行号作为唯一 ID，因为用户插入行后会导致素材被错误识别为新内容；
- 生成 ID 的记录在预览中标记“系统生成 ID”；
- 同一内容在多 Sheet 出现时，先提示潜在重复，不要擅自删除；
- 用户确认时允许编辑 ID，但最终仍须通过当前 ID 规则和唯一性校验。

### 5.6 智能能力边界

- 默认优先使用本地规则、别名词典、Sheet 名称、样本类型和确定性归一化，不调用网络；
- 可提供“AI 辅助映射”按钮，但必须是用户主动开启的可选功能；
- AI 辅助只接收列名、Sheet 名和最少量脱敏样例，只返回结构化映射 JSON；
- AI 不得生成素材正文、成果、数字、日期或类型事实；
- AI 返回的映射必须再次经过服务端 schema 校验，未知字段、越权字段和幻觉内容直接拒绝；
- UI 明确提示：开启 AI 辅助映射可能把样例数据发送到当前配置的 AI 服务，默认关闭。

---

## 6. 导入向导与 API

### 6.1 暂存目录和会话

新增本地暂存目录，例如：

```text
data/resume_material_imports/<import_id>/source.xlsx
data/resume_material_imports/<import_id>/analysis.json
```

要求：

- `import_id` 使用不可预测的随机 ID，不使用用户文件名直接作为路径；
- 文件名只用于展示，不能参与路径拼接；
- 暂存目录必须位于 `DATA_DIR` 内；
- 确认时重新读取文件并校验 SHA-256 与分析时一致；
- 分析取消、过期会话和失败暂存按 TTL 清理；
- 未确认暂存文件不参与简历生成；
- 暂存原始 Excel、分析 JSON 和日志不进入 Git。

### 6.2 新增 API

建议新增以下接口，字段名可微调，但语义必须保持：

#### `POST /api/resume/materials/analyze`

上传 Excel，仅扫描并生成导入会话，不覆盖正式库。

返回至少包含：

```json
{
  "success": true,
  "import_id": "...",
  "filename": "我的经历.xlsx",
  "source_sha256": "前12位",
  "sheets": [
    {
      "name": "项目经历",
      "hidden": false,
      "header_row": 3,
      "columns": [],
      "mapping": [],
      "preview": [],
      "row_count": 12,
      "valid_row_count": 10,
      "warning_count": 2
    }
  ],
  "warnings": [],
  "errors": [],
  "requires_confirmation": true
}
```

限制：只接受允许的 Excel 类型和大小；不执行公式、宏、外部链接或嵌入对象。

#### `POST /api/resume/materials/confirm`

请求体至少包含：

```json
{
  "import_id": "...",
  "source_sha256": "分析时的完整 hash 或服务端会话 hash",
  "sheets": [
    {
      "name": "项目经历",
      "include": true,
      "header_row": 3,
      "field_mapping": {
        "项目名称": "title",
        "详细描述": "description"
      },
      "type_override": "project",
      "excluded_rows": [7, 9]
    }
  ],
  "defaults": {
    "resume_allowed": true,
    "priority": 3
  }
}
```

服务端必须重新扫描、重新转换和严格校验，而不是信任浏览器传回的预览结果。成功后才调用原子保存，返回正式素材库数量、hash、警告和生成 ID 统计。

#### `DELETE /api/resume/materials/import/<import_id>`

取消并删除当前用户的本地暂存会话。越界 ID、已提交 ID 和不存在 ID 要有明确响应，不得删除正式素材库。

### 6.3 现有接口处理

- `GET /api/resume/materials/status`：增加 `import_in_progress`、`stale`、`source_sha256`、`last_import_mode` 等状态字段，但不要返回个人素材正文；
- `GET /api/resume/materials`：继续只返回已确认正式索引，并继续过滤 `source`、`notes`；
- `POST /api/resume/materials/refresh`：用于外部编辑正式 XLSX 后重建索引，不能代替未知文件的分析确认；
- `GET /api/resume/materials/template`：继续提供标准模板，并在模板说明中写明可直接上传其他 Excel；
- `POST /api/resume/materials/upload`：保留兼容。标准模板可走旧流程，但新前端上传任意 Excel 必须走 analyze/confirm；如果兼容接口也直接落库，必须只允许严格标准格式，不能把任意格式静默直写。

### 6.4 导入向导 UI

修改 `src/openjob/web/frontend/src/components/config/ResumeMaterials.tsx`，可以拆分成：

- `ResumeMaterials`：状态和正式库操作；
- `MaterialImportWizard`：分析、映射、预览、确认；
- `ImportMappingTable`：列映射编辑；
- `ImportPreviewTable`：行预览、警告和排除。

界面步骤：

1. **选择文件**：显示支持格式、大小限制、隐私说明；
2. **扫描中**：显示 Sheet 数、行数，不阻塞页面其他配置；
3. **字段映射**：每个原始列显示目标字段、置信度、推断原因和样例值；
4. **数据预览**：显示每个 Sheet 的类型、有效行、警告行、生成 ID、缺失字段、重复项；
5. **确认导入**：明确“确认后会替换当前正式素材库”，需要二次确认；
6. **完成**：显示导入条数、忽略条数、警告、来源 hash，并提供“查看素材库”和“重新导入”。

必须支持：

- Sheet 勾选/取消；
- 修改表头行；
- 修改列到标准字段的映射；
- 将一列设为忽略；
- 修改 Sheet 默认类型；
- 选择默认 `resume_allowed` 和 `priority`；
- 排除单行坏数据；
- 发现重复 ID 时返回到修正，不允许带重复项提交；
- 低置信度映射必须显眼，但不能用红色恐吓用户；
- 浅色、深色、窄屏下表格可横向滚动，不发生容器溢出；
- 失败时保留旧正式库和旧预览状态。

文案必须让普通用户理解：

> 系统会先读取表头和数据结构，推测哪些列对应标题、描述、成果等字段。请在确认前检查映射；系统不会替你编造缺失经历。

---

## 7. 转换、校验与安全要求

### 7.1 Excel 格式支持

第一阶段至少支持普通 `.xlsx`；如果要实现旧 Excel 的 `.xls`，增加 `xlrd>=2.0` 并封装为统一行读取接口。对于 `.xlsm`、带宏文件、加密文件、损坏文件、`.xlsb`，要么明确拒绝并告诉用户原因，要么增加经过评估的专用依赖，不允许伪装成导入成功。

至少要支持：

- 中文路径和空格路径；
- 多 Sheet；
- 隐藏 Sheet 的明确提示；
- 日期单元格、数字单元格和空单元格；
- 公式只读取缓存值，不执行公式；
- 合并单元格给出警告；
- 超过大小限制直接拒绝。

### 7.2 安全边界

- 文件大小限制沿用或明确提升当前 10MB 上限；
- 检查扩展名、文件签名和实际解析格式；
- 不执行宏、公式、外部链接、超链接目标或嵌入脚本；
- 不允许用户传入任意保存路径；
- 所有正式文件和暂存文件必须位于 `DATA_DIR`；
- 目录边界用 `resolve()` + `relative_to()`，禁止 `startswith()`；
- 失败上传不能覆盖合法文件；
- 原子写入 XLSX 和 index，临时文件最终只在目标目录内替换；
- 清理暂存目录时再次确认最终绝对路径处于 `DATA_DIR/resume_material_imports` 内；
- 日志中不能打印完整个人经历、证明路径、联系方式或文件绝对路径。

### 7.3 严格校验规则

确认提交时必须校验：

- 至少一条有效素材；
- `title` 和事实正文存在；
- `id` 合法且唯一；
- `type` 能映射为标准枚举；
- 日期可解析或用户明确选择保留为文本；
- `end_date` 不早于 `start_date`；
- `resume_allowed` 和 `priority` 合法；
- 长文本不超过当前 schema 上限；
- 同一个源单元格不能被错误重复拼入多个字段；
- `source`、`notes` 只保存于本地索引/导入审计，不进入候选素材 prompt。

某一行失败时提供“排除此行后继续”的用户选择；默认不允许把坏行静默丢弃。用户确认后被排除的行必须记录在导入审计中。

---

## 8. 简历生成链路必须真正使用导入结果

完成导入后，必须端到端验证：

1. 生成阶段通过统一 `resolve_library_paths(config, DATA_DIR)` 找到正式库；
2. index hash 与当前 XLSX 不一致时，明确刷新或失败，不使用错误旧索引；
3. 只把 `resume_allowed=true` 的标准素材参与候选匹配；
4. 候选匹配继续使用 JD 标题、摘要、关键词、硬性技能、加分技能、方向和素材字段；
5. AI prompt 不包含 `source`、`notes`、证明路径和导入内部审计；
6. 每个新增事实的 `SectionChange` 必须带 `material_ids`；
7. 仍限制最多 4 条选中素材、最多 3 个使用新增素材的简历变量行；
8. 服务端验证素材 ID 必须属于本次候选集；
9. 简历版本保存选中素材的脱敏快照和导入 hash，以便用户查看来源；
10. 用户不采纳某个改写块时，该块对应的素材不能被宣称为已使用；
11. 关闭素材库或尚未确认导入时，旧底稿生成行为不变。

重点回归：不能出现“配置页显示已导入，但生成结果和审计为空”，也不能出现“生成读取了另一份默认文件”。

---

## 9. 测试任务

### 9.1 领域层单元测试

新建或扩展 `tests/test_resume_materials_import.py`，至少覆盖：

- 标准 16 列英文模板识别；
- 中文列名和不同列顺序；
- 表头位于第 2、3、5 行；
- 多 Sheet 合并导入；
- Sheet 名推断类型；
- 类型列覆盖 Sheet 默认类型；
- 中文日期、Excel 日期单元格、`至今`；
- 技能的逗号、中文逗号、顿号、分号、换行；
- 不把版本号、路径或短语中的 `/` 错拆；
- 无 ID 时生成稳定 ID，重新导入同文件 ID 不变；
- 同内容跨 Sheet 的潜在重复提示；
- 缺标题、缺事实描述、重复 ID、非法日期的错误；
- 示例行自动排除；
- `source`、`notes` 不进入 prompt；
- 空 Sheet、隐藏 Sheet、合并单元格、说明行；
- `.xlsx` 正常解析、非法文件、超大文件、宏文件拒绝；
- 中文路径和空格路径；
- 分析阶段不写正式库；
- 未确认会话不能被简历生成读取；
- 确认时源文件 hash 改变会拒绝提交；
- 提交失败时旧 XLSX、旧 index、旧列表完全不变；
- 原子写入失败时不留下可被误读的半成品。

### 9.2 API 测试

扩展 `tests/test_web_api_routes.py`，至少覆盖：

- `analyze` 返回 Sheet、表头、映射、预览和警告；
- 任意中文 Excel 不再因固定 `素材库` Sheet 失败；
- `confirm` 只使用用户确认的 Sheet、列和行；
- 映射修改后标准字段正确落库；
- 低置信度但用户确认后可以提交；
- 未确认不能看到正式列表，也不能参与生成；
- 重复 ID 返回 400，旧库保留；
- 非法映射目标返回 400；
- 过期 `import_id`、错误 hash、越界会话被拒绝；
- `/upload` 旧接口兼容测试继续通过；
- status stale、refresh、delete 行为正确；
- 自定义素材路径时 API 和 engine 使用同一文件；
- 删除不能越界；
- API 不返回 `source`、`notes`、暂存绝对路径或完整私密内容。

### 9.3 简历引擎回归测试

扩展 `tests/test_resume_engine.py`：

- 导入一份非标准中文多 Sheet Excel；
- 确认后生成 JD 匹配结果；
- 断言候选集中出现导入的真实标题/ID；
- 断言生成 prompt 没有 source/notes；
- 断言版本审计包含被引用素材快照；
- 断言关闭开关时不读取素材库；
- 断言未确认导入不影响旧流程；
- 断言自定义路径、stale 索引和失败刷新行为。

### 9.4 前端构建与手工验收

```bash
python -m pytest -q
cd src/openjob/web/frontend
npm run build
cd ../../..
git diff --check
git status --short
```

浏览器验收至少包含：

1. 下载模板并导入；
2. 导入一份只有“项目经历/奖项/学生工作”三个 Sheet 的中文 Excel；
3. 导入一份表头从第 3 行开始的 Excel；
4. 在映射页手动修改“成果”和“描述”列；
5. 排除一行坏数据后确认；
6. 外部修改正式 XLSX，看到“索引过期”，刷新后恢复；
7. 上传非法文件，旧库仍可用；
8. 生成定制简历，查看素材审计；
9. 确认最多 4 条素材和最多 3 个素材改写块限制仍有效；
10. 浅色、深色、窄屏下向导和预览表格没有溢出；
11. 关闭素材库，确认旧生成流程仍能工作；
12. 确认投递/发送仍需要人工确认。

---

## 10. 推荐实现顺序

- [ ] **阶段 0：先锁定现状**
  - 运行现有测试；
  - 固化 API/engine 路径一致性、stale、数量限制和旧模板兼容测试；
  - 备份并只读检查当前 `data/`，不要把真实内容写入测试。

- [ ] **阶段 1：抽离智能导入领域层**
  - 实现文件读取、Sheet 扫描、表头识别、别名映射、类型推断、稳定 ID、预览和严格转换；
  - 保留 `parse_workbook()` 作为最终标准校验入口；
  - 先写失败测试，再实现。

- [ ] **阶段 2：实现暂存和确认 API**
  - 实现 `analyze`、`confirm`、取消和 TTL 清理；
  - 接入 hash、路径安全、原子提交和失败回滚；
  - 让旧 upload 接口兼容，但禁止任意格式绕过确认。

- [ ] **阶段 3：实现导入向导 UI**
  - 上传、扫描、映射、预览、确认、结果和错误状态；
  - 支持修改映射、Sheet 类型、排除行和默认字段；
  - 更新状态文案和响应式布局。

- [ ] **阶段 4：修复现有素材库确定性问题**
  - engine/API/refresh/delete 全部调用公共 `resolve_library_paths()`；
  - 修复示例行；
  - 修复删除目录边界检查；
  - 检查 4 条/3 个改写块的服务端硬限制；
  - 修复转换脚本的兜底虚构文本。

- [ ] **阶段 5：端到端接入与验证**
  - 确认导入内容能进入 JD 候选、AI prompt、版本审计；
  - 跑全量测试、前端构建和浏览器验收；
  - 检查 Git diff，不提交任何个人素材。

---

## 11. 完成判定（不可用“能上传”代替）

只有以下条件全部满足，才算完成：

- [ ] 其他用户无需使用作者专属 Excel，即可导入自己的常见 Excel；
- [ ] 至少支持中文表头、不同列顺序、多 Sheet、表头前说明行；
- [ ] 上传先分析，不直接覆盖；
- [ ] 用户可以看到并修改字段映射；
- [ ] 用户确认后才生成标准素材库；
- [ ] 无法识别和数据风险有明确警告，不能静默猜测；
- [ ] AI 不生成任何来源 Excel 中不存在的事实；
- [ ] 原始 Excel 和内部审计只留本地；
- [ ] 非法导入不会损坏旧库；
- [ ] 标准模板、旧 API、旧素材库和关闭素材库开关的流程兼容；
- [ ] API、engine、refresh、delete 使用同一个安全路径；
- [ ] 导入后的素材实际参与 JD 匹配和简历优化；
- [ ] 选中素材、改写块数量限制仍在服务端生效；
- [ ] `python -m pytest -q` 全部通过；
- [ ] 前端 `npm run build` 通过；
- [ ] 浏览器完成上述 12 项验收；
- [ ] `git diff --check` 通过，`git status` 中没有个人数据和暂存文件。

---

## 12. Agent 完成后必须报告

请按以下格式返回，不要只说“已完成”：

```text
## 实施结果
- 新增/修改文件：
- 是否支持 .xlsx：是/否
- 是否支持 .xls：是/否；依赖是什么
- 是否支持多 Sheet、中文表头、表头偏移：是/否
- 是否实现分析→映射→预览→确认：是/否
- 是否修改真实个人素材：是/否；如是说明原因

## 验证结果
- python -m pytest -q：
- npm run build：
- git diff --check：
- 浏览器验收：

## 已修复问题
- [ ] API/engine 素材路径统一
- [ ] 任意 Excel 智能扫描和字段映射
- [ ] 导入预览和用户确认
- [ ] 暂存、hash 校验和原子提交
- [ ] 示例行不会进入正式素材库
- [ ] stale 索引提示和刷新
- [ ] 服务端 4 条素材 / 3 个改写块限制
- [ ] source/notes 隐私边界
- [ ] 旧模板、旧 API、关闭开关兼容

## 未解决或需用户确认
- 仍不支持的 Excel 格式：
- 低置信度字段或需要用户确认的数据：
- 依赖安装、部署或迁移注意事项：
```

