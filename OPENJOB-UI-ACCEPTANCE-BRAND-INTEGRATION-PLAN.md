# OpenJob UI 第二轮验收与品牌 Logo 接入计划

> 文档类型：验收报告 + 下一轮 UI 优化实施方案  
> 验收对象：OpenJob 前端工作台第二轮 UI 改造结果  
> 验收基准日期：2026 年 9 月 2 日  
> 当前要求：本轮只输出方案与验收结论，不直接修改 UI 源代码。

---

## 1. 验收范围与方法

### 1.1 验收范围

已覆盖以下页面与主题：

- 工作台：`/`
- 岗位池：`/jobs`
- 投递确认：`/confirm`
- 简历工作台：`/resume`
- 市场分析：`/stats`
- 监测执行：`/monitor`
- 配置：`/config`
- 浅色主题与深色主题
- 1280×720 默认桌面视口
- 1024×768 中等视口
- 390×844 移动视口
- 新品牌资源：
  - `src/openjob/web/frontend/public/brand/openjob-icon.svg`
  - `src/openjob/web/frontend/public/brand/openjob-logo.svg`
  - `src/openjob/web/frontend/public/brand/openjob-logo-dark.svg`

### 1.2 验收方式

- 执行前端生产构建：`npm run build`
- 通过本地服务访问实际页面，而非只检查源代码
- 检查页面 DOM、可访问性语义、视口尺寸、横向溢出和关键组件尺寸
- 对投递详情仅进行打开、关闭、Esc 关闭等无业务副作用操作
- 未点击“一键投递”、真实发送、删除岗位等会改变业务数据的操作
- 检查浏览器控制台警告和错误

---

## 2. 总体验收结论

### 2.1 结论：有条件通过，建议进入品牌接入与体验收口阶段

本轮 Agent 的第二轮 UI 改造已经基本完成了原计划中的主要结构性工作：

- 首页 Bento 信息架构已经补全；
- 首页已经拆分为多个职责明确的卡片组件；
- 市场分析页完成了较大幅度的结构重构；
- 岗位池和投递确认页的筛选、批量操作、分页结构已经清晰很多；
- 投递确认详情弹窗已经具备 `role="dialog"`、`aria-modal="true"`，弹窗内容可以独立滚动；
- 详情弹窗可以通过关闭按钮和 `Esc` 关闭；
- 移动端已经有 BottomNav，页面没有出现文档级横向溢出；
- 图表相关代码已经懒加载并完成分包；
- 配置页、市场分析页、监测执行页的空状态和卡片结构已经统一；
- 生产构建通过，当前验收页面没有发现浏览器控制台错误或警告。

因此，不建议返工第二轮整体 UI。下一阶段应聚焦于：

1. 修复移动端岗位池的实际可用性；
2. 统一深色主题中的反色卡片；
3. 处理日期/时区口径；
4. 接入新的 OpenJob 图标与横向 Logo；
5. 进一步收紧二级页面标题和空状态体验。

### 2.2 完成度估计

- 结构与信息架构：约 90%
- 桌面端视觉方向：约 88%
- 深色主题一致性：约 82%
- 移动端实际可用性：约 75%
- 品牌资源接入：目前约 10%（资源已存在，但尚未接入页面）
- 工程构建与分包：约 90%

---

## 3. 验收通过项

### 3.1 首页工作台

**通过内容：**

- Hero、近 7 日行动、今日自动化三个核心模块已形成清晰的首屏层级；
- 今日求职流程和任务状态被压缩为可扫描模块；
- 待确认投递、最佳匹配、趋势、AI 用量等二级信息已经补回；
- 首页的主要入口仍然存在：运行全流程、单独采集、开启监测；
- 页面在 1024px 下可以正常堆叠，没有文档级横向溢出。

**需要后续优化：**

- 深色主题的“今日自动化”卡片使用 `bg-ink`，在深色 Token 下变成很亮的浅色卡片，和周围深色卡片不一致；
- 当前页面显示 `2026 年 9 月 3 日星期四`，而本次验收基准日期为 `2026 年 9 月 2 日`，需要确认是时区、固定 mock 数据，还是日期口径问题；
- 宽屏下可以继续提升内容利用率，让品牌 Logo 和关键状态更有存在感。

### 3.2 投递确认页

**通过内容：**

- 142 条待确认岗位已经分页为 `第 1 / 9 页`，没有一次性把所有岗位都渲染到当前页；
- 筛选项、批量操作、岗位卡片层级清晰；
- 详情弹窗实际位于视口中央，尺寸约为 768×634px；
- 弹窗具备：
  - `role="dialog"`
  - `aria-modal="true"`
  - 独立滚动区域
  - 关闭按钮
  - `Esc` 关闭
- 本轮验收没有执行真实投递动作。

**需要后续优化：**

- 移动端投递确认卡片的文字密度仍偏高，评分理由和缺失信息应支持“摘要 + 展开详情”；
- 详情弹窗可进一步增加“岗位基本信息 / 匹配分析 / 风险提示”三个分区，降低长段落阅读压力。

### 3.3 岗位池

**通过内容：**

- 桌面端筛选区、批量操作区、表格区结构已经收口；
- 公司、职位、城市、薪资、评分、状态、时间、操作列均有明确位置；
- 406 条数据已经分页为 28 页；
- 表格容器使用了横向滚动容器，桌面端不会造成文档级横向溢出。

**需要重点优化：**

- 390px 移动端仍然实际渲染宽度约 933px 的表格；
- 1024px 视口下表格实际宽度约 1106px，依赖内部横向滚动；
- 移动端截图中主要只能看到“选择”和“岗位”两列，城市、薪资、评分、状态、操作等关键信息被挤到不可见区域；
- 这说明当前虽然存在 `JobCards` 组件，但运行时在移动断点仍没有完全切换为岗位卡片。

这项问题不属于构建阻断，但会直接影响移动端核心任务完成效率，列为 **P1**。

### 3.4 简历工作台

**通过内容：**

- 页面标题、岗位选择、按 JD 优化入口都已存在；
- 空状态文案明确说明了下一步操作；
- 页面无横向溢出。

**需要后续优化：**

- 空状态仍然偏稀疏，首屏下方存在大面积空白；
- 建议补充三步引导：上传简历 → 选择岗位 → 生成/导出定制简历；
- 没有岗位时可以放置“去岗位池挑选岗位”的主 CTA，而不是只保留一句说明文字。

### 3.5 市场分析页

**通过内容：**

- 总岗位数、最集中城市、主流薪资、主流学历等关键指标已经形成统计卡片；
- 城市、学历、招聘类型、薪资、公司、来源平台、技能和福利关键词均已进入统一分析布局；
- 图表能够正常加载，颜色基本统一；
- 图表已经拆分为独立 chunk，构建产物中没有继续把所有图表代码塞入入口包。

**需要后续优化：**

- 统计页顶部 Header 标题与内容区标题均为“市场分析”，语义重复；
- 建议保留一个主标题，另一个位置改为当前筛选范围或更新时间；
- 空数据时建议统一显示“暂无足够数据”的图表占位，而不是只留下空卡片。

### 3.6 监测执行页

**通过内容：**

- 待处理、简历请求、自动跟进、已回复四个 Tab 已明确；
- 空状态说明了监测运行逻辑；
- 页面没有横向溢出。

**需要后续优化：**

- Header 标题与内容区标题同样存在重复；
- 空状态虽然完整，但缺少直接的下一步入口；
- 建议加入“去配置监测规则”或“启动一轮监测”的次级 CTA。

### 3.7 配置页

**通过内容：**

- 个人信息、简历上传、多方向简历、城市、评分、AI、反监测、监控、跟进、去重等设置已经形成统一长页面；
- 浅色和深色主题下输入区、上传区、列表卡片均可读；
- 长页面滚动正常，未发现文档级横向溢出。

**需要后续优化：**

- 配置页是最适合展示横向品牌 Logo 的页面之一，目前仍未接入；
- 建议在“个人信息”区顶部加入品牌识别，但不要让 Logo 抢过用户简历配置本身；
- 后续应继续检查所有图标按钮的 `aria-label`，尤其是删除、收藏、刷新、折叠等操作。

---

## 4. 问题分级与修复建议

## 4.1 P0：阻断问题

### 当前结论：未发现新的 P0 阻断问题

本次没有发现以下问题：

- 页面无法加载；
- 前端生产构建失败；
- 详情弹窗跑出视口；
- 详情弹窗无法关闭；
- 投递确认页一次性渲染全部数据导致页面不可用；
- 文档级横向溢出导致整页被撑宽；
- 深色主题完全不可读；
- 浏览器控制台出现运行时错误。

---

## 4.2 P1：核心体验问题

### P1-1：移动端岗位池仍使用超宽表格

- **页面：** `/jobs`
- **视口：** 390×844、1024×768
- **现象：** 移动端实际表格宽度约 933px；1024px 下约 1106px，虽然被放在 `overflow-x-auto` 容器中，但关键列需要横向拖动查看。
- **用户影响：** 用户无法在一屏内同时看到职位、薪资、评分、状态和操作；岗位池这个核心工作流在手机上扫描效率较低。
- **建议：**
  1. `lg` 以下直接渲染 `JobCards`，不要仅依赖表格横向滚动；
  2. 卡片第一行：公司 + 职位 + 评分；
  3. 第二行：城市 + 薪资 + 状态；
  4. 第三行：来源、活跃度、采集时间；
  5. 操作按钮放入底部操作栏或更多菜单；
  6. 批量选择保留在卡片左上角，批量工具栏改为 sticky bottom sheet 或顶部浮动条。

### P1-2：深色主题的今日自动化卡片反色过强

- **页面：** `/`
- **现象：** 深色主题下“今日自动化”卡片使用 `bg-ink`，由于 `--ink` 在 dark Token 中被定义为浅色，卡片变成接近白色；周围卡片则为深色。
- **用户影响：** 页面视觉重心被一张反色卡片强行吸走，且与当前深色 App Shell 的高级、克制方向不完全一致。
- **建议：**
  - 深色主题保留深色卡片，使用 `surface-strong` 或深色渐变；
  - 如果确实需要重点卡片，改为“深靛蓝底 + 蓝紫边缘光”，不要使用纯白；
  - 主按钮可以保留提亮后的钴蓝 `#6082FF`；
  - 通过 `dark:` 变体明确覆盖背景、文字、分隔线和按钮状态。

### P1-3：日期与验收基准存在一天偏移风险

- **页面：** 首页及 Header 时间区域
- **现象：** 本次验收基准日期为 2026 年 9 月 2 日，页面显示 2026 年 9 月 3 日星期四。
- **用户影响：** 求职行动、近 7 日趋势、发送窗口和自动化任务都依赖“今天”，日期偏移可能导致用户误判任务是否属于当天。
- **建议：**
  1. 明确产品统一使用浏览器本地时区、系统时区还是后端时区；
  2. 后端返回服务端当前时间和时区；
  3. 前端显示时给出必要的时区口径；
  4. mock 数据不要硬编码到未来日期；
  5. 增加跨午夜和不同地区时区的测试。

---

## 4.3 P2：视觉一致性与体验细节

### P2-1：新品牌资源已存在，但项目尚未实际使用

- **现象：** 当前 Sidebar 左上角仍然显示文字 `OJ`，源码中没有发现对 `/brand/openjob-icon.svg`、`openjob-logo.svg`、`openjob-logo-dark.svg` 的实际引用。
- **用户影响：** 视觉系统已经完成，但产品没有建立稳定的品牌识别；侧栏图标和新设计资源不一致。
- **建议：** 按第 7、8 节方案接入。

### P2-2：市场分析与监测执行存在标题重复

- **页面：** `/stats`、`/monitor`
- **现象：** Header 上显示页面标题，内容区又显示同名主标题。
- **用户影响：** 首屏信息密度被重复标题占用，层级感不够干净。
- **建议：**
  - Header 只保留面包屑、上下文或更新时间；
  - 内容区保留唯一 `h1`；
  - 如果 Header 必须显示标题，内容区改为“数据概览”“待处理队列”等二级标题。

### P2-3：简历空状态仍显得过空

- **页面：** `/resume`
- **现象：** 页面主要只有选择岗位、优化按钮和两行说明，下方有大面积留白。
- **用户影响：** 用户不容易理解完成简历工作流需要几步，也缺少明确的下一步行动。
- **建议：** 增加轻量流程卡、文件状态、最近版本和前往岗位池的 CTA。

### P2-4：品牌渐变与现有钴蓝体系需要职责分离

- **现象：** Logo 使用蓝 → 紫 → 粉、文字使用紫 → 粉 → 橙，而当前产品交互主色是钴蓝。
- **用户影响：** 如果把渐变直接扩散到按钮、状态、图表和导航，页面会从克制的工作台变成装饰性较强的彩色界面。
- **建议：**
  - 交互主色继续使用当前 cobalt blue；
  - 新渐变只用于品牌 Logo、重点 Hero、空状态插画、少量数据强调；
  - 禁止用品牌渐变表达成功、失败、警告等业务状态；
  - 图表最多使用一处渐变，其余数据系列使用中性蓝灰和辅助色。

---

## 4.4 P3：工程与长期维护建议

### P3-1：统一品牌组件封装

建议新增一个统一品牌组件层，例如：

- `BrandMark`：方形图标；
- `BrandLogo`：横向 Logo；
- `BrandFavicon`：浏览器图标仅通过 HTML head 接入；
- `BrandLockup`：图标、文字、产品副标题组合。

禁止在多个页面直接散落 `/brand/*.svg` 字符串，以免未来更换资源时需要全局搜索替换。

### P3-2：SVG 使用规范

- 当前 SVG 作为外部 `<img>` 使用时，`title` 和 `aria-labelledby` 基本可用；
- 如果未来改成 inline SVG，需要避免多个相同 `id="title"` 和 `id="flight"` 造成引用冲突；
- 应考虑给外部 `<img>` 提供明确 `alt`；装饰性重复图标使用空 `alt`；
- 可在组件层设置 `width`、`height`、`display:block` 和 `object-fit:contain`，避免图片基线抖动；
- SVG 没有单色降级版本，建议后续补齐高对比度、打印和极小尺寸版本。

### P3-3：动效与无障碍

- 保留当前 150–300ms 的短动效方向；
- 深色主题切换、弹窗、卡片入场都应继续支持 `prefers-reduced-motion`；
- 补充键盘焦点环、Tab 顺序和移动端底部导航的可访问性测试；
- 图表需要有文字摘要或表格降级，不能只依赖图形表达数据。

### P3-4：建立最小视觉回归矩阵

建议每次 UI 修改至少自动截图检查：

- `/`：light / dark，1280px / 390px；
- `/jobs`：light / dark，1280px / 390px；
- `/confirm`：light / dark，1280px / 390px；
- `/stats`、`/monitor`、`/config`：light / dark，1280px。

---

## 5. 新 Logo / Icon 视觉适配评估

## 5.1 图标形状：适配度高

`openjob-icon.svg` 的核心造型是纸飞机/飞行箭头，具有以下产品语义：

- 投递：把求职意向发送出去；
- 行动：从采集、评分到投递的连续推进；
- 速度：更快找到合适岗位；
- 自动化：任务沿着明确方向执行。

这个形状和 OpenJob 的业务比抽象字母 Logo 更容易建立记忆点，建议作为产品的主图形资产。

### 形状落位建议

- 侧栏主标：28–32px；
- Favicon：16px、32px、48px；
- 移动端品牌标：28px 左右；
- 空状态插画：48–96px；
- 登录/欢迎页：64–96px。

不建议在 16px 以下强行使用完整线条版本。白色内部线条和黄色圆点在极小尺寸下可能变细或丢失，应准备简化或单色版本。

## 5.2 颜色：适配，但必须控制使用范围

### 图标渐变

- 蓝色：`#1467F8`
- 紫色：`#7257E8`
- 粉色：`#F47E9C`
- 黄色节点：`#FFD176`

### 浅色横向 Logo 文字

- `Open`：`#101828`
- `Job` 渐变：`#5B35D8` → `#B552E0` → `#FFA968`

### 深色横向 Logo 文字

- `Open`：`#FFFFFF`
- `Job` 渐变：`#A793FF` → `#E582E5` → `#FFC184`

### 与现有 UI 的关系

当前产品交互主色是钴蓝，建议保持：

- 浅色交互主色：当前 `#3157E8`；
- 深色交互主色：当前 `#6082FF`；
- 品牌图形蓝：`#1467F8`；
- Logo 渐变仅承担品牌识别，不承担所有交互语义。

结论：颜色适合品牌，但不建议把 Logo 渐变直接用于所有按钮、Tab、状态和图表。

## 5.3 浅色主题适配

`openjob-logo.svg` 的深色 `Open` 与浅色 App Shell、浅灰画布和当前黑色主按钮相容；Logo 图标部分的蓝紫粉渐变可以作为首屏或品牌区域的视觉焦点。

建议优先使用场景：

- 登录/欢迎页；
- 配置页个人信息区域；
- 空状态或首次使用引导；
- 导出、分享、关于 OpenJob 等品牌场景。

不建议直接塞进 76px 宽的侧栏，因为横向 Logo 在侧栏中只能被压缩到不可读。

## 5.4 深色主题适配

`openjob-logo-dark.svg` 的白色 `Open` 对深色 App Shell 具有足够识别度，`Job` 的浅紫、粉、橙渐变也比浅色版更适合暗背景。

建议：

- 深色侧栏使用 `openjob-icon.svg`，外层放在深色或半透明品牌底上；
- 深色品牌页或配置品牌区域使用 `openjob-logo-dark.svg`；
- 不要把完整横向 Logo 放到深色窄侧栏；
- 高对比度模式下提供纯白或纯黑单色降级。

## 5.5 字体与形状一致性

横向 Logo 内部使用 `Arial, Helvetica, sans-serif`，跨平台稳定，但与应用主体的 Inter / HarmonyOS Sans SC / Microsoft YaHei 字体栈不完全一致。

建议：

- 第一阶段先以外部 SVG 原样使用，确保品牌字形稳定；
- 不要把 Logo 文字拆成 HTML 文本后重新排版；
- 如果未来要求品牌字体与产品字体完全统一，再单独制作新的品牌 SVG 资源，而不是用 CSS 覆盖 SVG 内部文字。

---

## 6. 推荐品牌 Token

建议新增独立的品牌 Token，不覆盖现有 UI 交互 Token：

```css
:root {
  --brand-blue: 20 103 248;       /* #1467F8 */
  --brand-purple: 114 87 232;      /* #7257E8 */
  --brand-pink: 244 126 156;      /* #F47E9C */
  --brand-orange: 255 169 104;     /* #FFA968 */
  --brand-yellow: 255 209 118;     /* #FFD176 */
  --brand-gradient: linear-gradient(135deg, #1467F8 0%, #7257E8 50%, #F47E9C 100%);
  --brand-word-gradient: linear-gradient(135deg, #5B35D8 0%, #B552E0 50%, #FFA968 100%);
}

html.dark {
  --brand-word-gradient: linear-gradient(135deg, #A793FF 0%, #E582E5 50%, #FFC184 100%);
}
```

注意：如果项目使用 Tailwind RGB Token，建议把颜色存为 RGB 三元组，以便继续支持 alpha-value 语法；Logo 本身仍直接引用 SVG，不需要把 SVG 渐变复制成 CSS。

---

## 7. Logo 接入实施方案

## 7.1 第一阶段：Sidebar 主图标

把当前 Sidebar 左上角的文字 `OJ` 替换为：

```tsx
<img src="/brand/openjob-icon.svg" alt="OpenJob" />
```

建议样式：

- 外层：`h-11 w-11`，与现有侧栏结构保持一致；
- 图标：`h-8 w-8` 或 `h-9 w-9`；
- 不要给图标强行套纯黑背景，优先使用透明图标 + 轻微阴影；
- 深色主题可以在图标外增加半透明深色圆角底，确保白色内部线条不融入背景；
- Hover 时只做轻微缩放或阴影变化，不让渐变发生大幅移动。

## 7.2 第二阶段：Header / 宽屏品牌区域

当前顶部 Header 主要承担导航，不建议直接把完整横向 Logo 放入所有页面。

推荐规则：

- 宽屏欢迎页或登录页：使用完整横向 Logo；
- 配置页“个人信息”区域：可使用宽度 140–180px 的横向 Logo；
- 普通业务页 Header：仍以页面标题和导航为主；
- 如果必须在 Header 放 Logo，只在 `xl` 宽度显示横向 Logo，窄屏自动切回 Icon。

可以采用 `<picture>` 或基于主题的资源选择：

```tsx
<picture>
  <source media="(prefers-color-scheme: dark)" srcSet="/brand/openjob-logo-dark.svg" />
  <img src="/brand/openjob-logo.svg" alt="OpenJob" />
</picture>
```

如果项目支持手动主题切换，优先由 React 主题状态选择资源，而不是只依赖系统媒体查询。

## 7.3 第三阶段：Favicon 与页面元信息

在前端 HTML 入口中加入：

```html
<link rel="icon" type="image/svg+xml" href="/brand/openjob-icon.svg" />
```

同时可以补充：

- Apple touch icon；
- 主题色 `theme-color`；
- 分享卡片或桌面快捷方式资源。

Favicon 只使用方形 Icon，不使用横向 Logo。

## 7.4 第四阶段：空状态、欢迎页和配置页

优先接入完整横向 Logo的场景：

1. 登录/欢迎页；
2. 首次进入工作台的空状态；
3. `/resume` 无简历时的引导区域；
4. `/config` 的个人信息/品牌区域；
5. `/monitor` 无监测任务时的引导区域。

使用方式：

- Logo 作为视觉锚点，不作为大面积背景；
- 最大宽度建议 160–220px；
- 图标和文字之间保持原 SVG 内部比例，不另行拆解；
- 空状态正文仍然保持简洁，避免 Logo 和说明文字同时抢主层级。

## 7.5 第五阶段：移动端

移动端顶部不建议放完整横向 Logo，因为宽度有限。建议：

- 顶部左侧显示 24–28px Icon；
- 页面标题居中或左对齐；
- BottomNav 保持当前图标导航，不重复放品牌字标；
- 在移动端空状态或设置页再使用横向 Logo。

## 7.6 资源封装与主题切换

建议建立统一资源映射：

```ts
export const brandAssets = {
  icon: '/brand/openjob-icon.svg',
  logoLight: '/brand/openjob-logo.svg',
  logoDark: '/brand/openjob-logo-dark.svg',
} as const
```

组件层应做到：

- 主题切换后 Logo 资源同步切换；
- 图片加载失败时回退到 `OJ` 文本或单色 Icon；
- 所有品牌图片都设置明确 `alt`；
- 装饰性重复图片使用空 `alt`；
- 不允许业务状态颜色覆盖 Logo 原始渐变。

---

## 8. 下一轮推荐实施顺序

### P0：先做视觉和数据口径风险收口

1. 确认日期、时区和 mock 数据的统一口径；
2. 修复深色主题“今日自动化”卡片的反色问题；
3. 重新验收深色首页和深色配置页。

### P1：修复核心响应式体验

1. 390px 以下岗位池切换为 `JobCards`；
2. 1024px 岗位池改为更紧凑的表格或卡片混合模式；
3. 移动端筛选器继续保持折叠按钮 + Bottom Sheet；
4. 移动端批量操作改为 sticky 工具栏。

### P2：接入品牌资源

1. Sidebar 使用 `openjob-icon.svg`；
2. Favicon 使用 `openjob-icon.svg`；
3. 配置页和空状态按主题使用 light/dark 横向 Logo；
4. 统一品牌 Token 和组件封装；
5. 删除所有页面中散落的 `OJ` 文本 Logo。

### P3：体验打磨

1. 收紧 `/stats`、`/monitor` 的重复标题；
2. 补强 `/resume` 和 `/monitor` 空状态 CTA；
3. 增加高对比度、键盘焦点和减少动效测试；
4. 增加视觉回归截图；
5. 增加图表无障碍文字摘要。

---

## 9. 最终验收标准

### 9.1 品牌接入

- [ ] Sidebar 不再显示纯文字 `OJ`，改为 Icon；
- [ ] Favicon 使用 `openjob-icon.svg`；
- [ ] 浅色主题横向 Logo 使用 `openjob-logo.svg`；
- [ ] 深色主题横向 Logo 使用 `openjob-logo-dark.svg`；
- [ ] Logo 在 16px、24px、32px、96px 下都能识别；
- [ ] 不把品牌渐变滥用到所有业务状态；
- [ ] Icon 和 Logo 具备正确 `alt` 与语义。

### 9.2 响应式

- [ ] 390px 岗位池使用卡片，不要求用户横向拖动超宽表格；
- [ ] 390px 页面无横向溢出；
- [ ] 1024px 首页 Bento 堆叠合理；
- [ ] 1024px 岗位池主要列可读；
- [ ] BottomNav 不遮挡最后一项内容；
- [ ] 顶部导航在小屏不出现逐字竖排或不可点击。

### 9.3 深色主题

- [ ] 首页所有卡片背景与深色 App Shell 一致；
- [ ] 主按钮、激活 Tab、输入框和禁用态可区分；
- [ ] 横向 Logo 根据主题切换；
- [ ] 图表 Tooltip、坐标轴、图例可读；
- [ ] 不依赖单纯红/绿颜色表达状态。

### 9.4 交互与无障碍

- [ ] 详情弹窗具备 dialog 语义；
- [ ] 弹窗支持关闭按钮、遮罩和 Esc；
- [ ] 弹窗内容独立滚动；
- [ ] 所有图标按钮有 `aria-label` 或可见文本；
- [ ] 键盘 Tab 顺序自然；
- [ ] 支持 `prefers-reduced-motion`；
- [ ] 图表有文字或表格摘要。

### 9.5 工程

- [ ] `npm run build` 通过；
- [ ] 页面加载无控制台错误；
- [ ] 品牌资源统一通过组件或资源映射管理；
- [ ] 图表继续保持懒加载或合理分包；
- [ ] 视觉回归截图覆盖 light/dark 与移动端。

---

## 10. 本次验收截图

以下截图已保存到项目目录，便于后续 Agent 对照：

- 桌面浅色首页：`C:\Users\暴龙战士wink\Desktop\agent\OpenJob\ui-acceptance-screenshots\desktop-home-light.png`
- 桌面深色首页：`C:\Users\暴龙战士wink\Desktop\agent\OpenJob\ui-acceptance-screenshots\desktop-home-dark.png`
- 移动端岗位池：`C:\Users\暴龙战士wink\Desktop\agent\OpenJob\ui-acceptance-screenshots\mobile-jobs-light.png`

重点参考：

- 桌面深色首页：查看“今日自动化”卡片与周围深色卡片的反差；
- 移动端岗位池：查看超宽表格在 390px 下只能看到前两列的问题；
- 桌面浅色首页：查看当前 `OJ` 文字标识和新 Icon 之间的差异。

---

## 11. 给执行 Agent 的简短指令

请不要重做第二轮 UI。按以下顺序做增量收口：

1. 先修复深色首页卡片反色和日期/时区口径；
2. 再让 390px 岗位池真正切换为 `JobCards`；
3. 接入 `openjob-icon.svg` 到 Sidebar 和 Favicon；
4. 接入浅色/深色横向 Logo 到配置页、欢迎页和空状态；
5. 统一品牌 Token，不改变现有钴蓝交互主色；
6. 处理 `/stats`、`/monitor` 标题重复与 `/resume` 空状态稀疏；
7. 完成 light/dark、1280/1024/390 的回归验收。

验收目标不是“让每个地方都出现 Logo”，而是让 OpenJob 形成：

> **钴蓝负责交互，纸飞机渐变负责品牌，黑白灰负责工作台秩序。**

这三层职责要保持清晰，才能同时拥有灵动感、高级感和长期可维护性。
